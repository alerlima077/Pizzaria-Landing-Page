/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

export default function App() {
  // The landing page is built directly in index.html as requested.
  // This React component is kept empty to avoid interfering with the static HTML structure.
  return null;
}
